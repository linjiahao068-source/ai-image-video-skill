# 验证记录

本页记录公开技能包在构建时的验证范围。它只说明方法是否被整理成可触发、可区分、可执行的技能；不将课程中的平台功能、模型效果、价格或案例结果视为永久事实。

## 三重验证标准

每个 skill 都按以下标准筛选与修订：

1. **V1 — 跨段证据**：方法至少由课程中的不同片段、案例或操作原则支撑，而非孤立的界面演示。
2. **V2 — 预测力**：方法能回答课程未逐字演示的新任务，例如把“动作难写”判断为应优先提供视频参考，而非继续堆叠文字。
3. **V3 — 非常识性**：方法必须给出可操作的决策、验收标准或边界，不能只是“提示词写详细一点”之类的泛泛建议。

## 通过验证的 skills

| 分组 | Skill | 核心可检验命题 | V1 | V2 | V3 |
| --- | --- | --- | --- | --- | --- |
| 图片提示词 | `image-prompt-specification` | 用时间、主体、场景、风格消除关键歧义，并按任务删减字段。 | 通过 | 通过 | 通过 |
| 图片提示词 | `reference-image-prompt-reverse-engineering` | 只从可见证据反推提示词，不将不可见内容伪装为事实。 | 通过 | 通过 | 通过 |
| 图片提示词 | `constraint-aware-prompt-expansion` | 先定义不可变约束，再决定探索或严格扩写。 | 通过 | 通过 | 通过 |
| 输入与控制 | `generation-mode-reference-selection` | 外观、动作、镜头、节拍应由不同输入模态直接承载。 | 通过 | 通过 | 通过 |
| 输入与控制 | `capability-cost-fit` | 模型能力档位应由失败风险与阶段决定，而非默认选最高规格。 | 通过 | 通过 | 通过 |
| 输入与控制 | `preserve-change-edit-contract` | 局部编辑须同时声明变更项与保留项。 | 通过 | 通过 | 通过 |
| 输入与控制 | `reference-anchor-density` | 高保真任务要用更多角度、端帧和细节降低模型猜测空间。 | 通过 | 通过 | 通过 |
| 视频工作流 | `video-direction-specification` | 抽象情绪和叙事意图需落成可见行为、镜头、效果与节奏。 | 通过 | 通过 | 通过 |
| 视频工作流 | `storyboard-event-budgeting` | 事件数必须匹配可用时长，复杂故事应拆段。 | 通过 | 通过 | 通过 |
| 视频工作流 | `endpoint-anchored-video-synthesis` | 关键收尾、变形完成态或循环落点需要端点锚定。 | 通过 | 通过 | 通过 |
| 视频工作流 | `sequence-continuity-assembly` | 成片连续性来自动作、空间、接缝与节拍的装配，而非片段直接堆叠。 | 通过 | 通过 | 通过 |

## 压力测试

- 共 **11** 个 skill，每个包含 **6** 条独立测试用例：应触发、应不触发和边缘条件各有覆盖。
- 合计 **66** 条用例；构建时各 skill 的 `test-results.md` 均记录为 **6/6 通过（100%）**。
- 两项早期边界用例曾因“正确路由到相邻 skill”而与原期望不一致：`storyboard-event-budgeting` 和 `sequence-continuity-assembly`。公开包已采用修订后的 `test-prompts.json`，使测试期望与技能边界一致。

## 验证的局限

这些测试验证的是方法的触发、选择与边界，不是对某个生成平台的画质、成本、功能可用性或商业效果的承诺。实际项目仍需要使用目标模型、素材和交付标准进行人工验收。
