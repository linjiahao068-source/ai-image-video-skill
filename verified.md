# 验证记录

本页记录仓库在构建时实际完成的验证，不把模型效果、价格、平台能力或未运行案例写成事实。

## 原有 11 个原子 Skill

原有统计保持独立：

- 11 个原子 Skill。
- 每个原子 Skill 6 条独立用例。
- 合计 **66 条**。
- 其原有 `test-results.md` 记录保持不变，不并入总控工作流测试数字。

这些测试覆盖触发、反例和边界，验证方法的路由与可执行性，不承诺任何生成平台的画质或商业效果。

## build-image-fast 总控工作流

### 已确认通过

- 官方 Skill 结构验证器 `quick_validate.py`。
- YAML、JSON 与 frontmatter 结构检查。
- Skill 内部 Markdown 链接检查。
- 14 项阶段合同静态检查。
- 18 条测试用例定义的 JSON 可解析性和覆盖检查。
- 股票四格案例的八阶段 dry run：确认门、版本锁定、回退、失效传播、候选续接、QA 和最终文件门均有对应规则。

### 暂未验证

- 没有在本次仓库构建中调用图片模型生成真实候选。
- 没有把 dry run 当成真实图片质量测试。
- 没有验证任何特定模型对中文排字、四格连续性或角色身份的一次通过率。
- 没有生成 Seedance 2.0 视频；V1 仅验证交接字段存在。

详情见 [`build-image-fast/test-results.md`](./build-image-fast/test-results.md)。

## 边界

- QA 门为 85/100 且无硬失败，但评分仍需要对真实交付物进行人工或可复核验收。
- 公开或商业发布涉及角色、品牌、肖像、参考素材和字体时，权利状态未确认即为发布阻断。
- 精确文字失败且没有可用排版工具时，工作流必须保持 `受阻/待排版`。
- 模型参数和能力可能变化；精确执行以当前已安装 `imagegen` Skill 为准。
