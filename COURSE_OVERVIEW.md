# 课程概览：从“会生成”到“能控制”的 AI 影像工作流

## 核心结论

这套方法不把 AI 影像创作理解为一次性写完提示词，而把它视为四个连续决策：

1. **输入职责**：最难控制的变量由最直接的模态承担——图像偏向外观和构图，视频偏向动作与镜头，音频偏向节拍，文字补充语义与缺失信息。
2. **变更与锚点**：局部编辑必须同时写清“改什么”和“保留什么”；主体、产品与跨镜头画面需要足够的角度、时间和细节锚点。
3. **镜头预算**：完整短片的最小单位是有时长预算的镜头与事件，不是无边界的提示词。故事复杂度超过时长时，应拆段生成。
4. **端点与装配**：当结尾、变形完成态或循环落点不可出错时，应提供尾帧或关键帧；片段生成后，仍需解决动作、空间、声音与节拍的接缝。

## 这套方法不主张什么

- 不主张默认使用最贵或最高规格的模型。能力选择应由失败风险和交付阶段决定。
- 不主张向提示词无限加形容词。关键是消除会影响成败的歧义，而不是长度。
- 不主张单张参考图承担高保真、多角度、跨镜头的一致性任务。
- 不主张把太多故事事件强塞进一次短时长生成。
- 不主张把每个生成片段各自配乐后直接硬拼成成片。

## 稳定适用范围

这些 skills 适用于图像、视频和音频辅助的前期设定、生成、局部编辑、分镜和后期装配。它们描述的是创作决策与验收逻辑，而不是某一平台的按钮位置、版本能力或积分规则。

## 已知边界

- 生成模型不是确定性渲染器；同一份输入仍可能需要迭代与人工验收。
- 真实人物、受保护角色、商标、客户素材和商业交付另有授权与合规要求。
- 不同平台对参考图、视频、音频、时长和编辑能力的限制不同，执行前应在目标平台实测。
- 当任务主要是纯平面排版、精确文字或像素级修图时，应选择更适合的设计或编辑工具，而非强行套用生成视频工作流。

## 构建方式

本仓库使用 cangjie-skill 的 RIA-TV++ 思路，将课程中反复出现、可跨情境解释的做法写成原子化 skill。每个公开 skill 保留：

- **R**：来源证据与上下文；
- **I**：可迁移的方法论骨架；
- **A1/A2**：课程案例与未来触发场景；
- **E**：可执行步骤和完成标准；
- **B**：不适用条件、失败模式与边界；
- **测试**：触发、反例和边缘条件的独立盲测。

这不是对课程完整内容的替代，也不包含原始课程素材。出处与许可边界见 [NOTICE.md](./NOTICE.md)。
