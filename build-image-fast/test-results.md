# build-image-fast V1 验证记录

验证日期：2026-07-29。

本文件把静态合同验证、情境演练和真实图片生成分开记录。未执行的项目不计为通过。

## 1. Skill 结构与静态合同

结果：**通过**。

- 官方 `quick_validate.py`：通过。为运行验证器补装了缺失的 PyYAML，并启用 UTF-8 读取。
- `SKILL.md`：123 行，小于 500 行。
- YAML frontmatter：只包含 `name` 与 `description`。
- `agents/openai.yaml`：YAML 可解析，默认提示包含 `$build-image-fast`，允许隐式调用。
- `test-prompts.json`：JSON 可解析，18 条用例，要求通过率 100%。
- Skill 内部 Markdown 链接：无断链。
- 定制阶段合同检查：14 / 14 通过。

定制检查覆盖：八阶段完整性、状态词表、未确认不出图、候选选择门、连续两轮硬失败回退、85 分 QA 门、最终文件确认门、单点任务路由、内置生成默认路径、`gpt-image-2` 精确回退、Seedance 仅交接、JSON、frontmatter 与内部链接。

## 2. 股票四格端到端情境演练

结果：**流程合同通过，生成结果未验证**。

使用“一买就跌、一卖就涨”的股票四格梗，逐段检查以下状态转换：

| 阶段 | 演练产物 | 合同判定 |
| ---: | --- | --- |
| 1 | 平台、市场颜色、受众、权利与规格清单 | 未确认不得进入 2 |
| 2 | 三个不同创意方向与推荐理由 | Agent 不代选 |
| 3 | 四格事件、逐字对白、买卖与涨跌因果 | 文案确认后才进入 4 |
| 4 | 原创角色、2×2 版式、阅读方向、身份锚点 | 参考职责逐项锁定 |
| 5 | 快速/稳定路线、完整与分格提示词、硬门槛 | 未确认不得出图 |
| 6 | `image-v01` 候选续接状态 | 用户未选不得进入 QA |
| 7 | 100 分制、85 分门槛、因果与文字硬失败 | 不通过不得交付 |
| 8 | Build Pack、最终路径、复现素材清单 | 文件未确认不得完成 |

这是一轮基于合同的 dry run，不是实际模型出图。它验证状态机和回退逻辑，不验证画质、文字准确率或模型可用性。

## 3. 真实图片生成

结果：**未执行，不计为通过**。

原因：本工作流要求用户在阶段 1–5 逐阶段确认具体项目版本，并在阶段 5 明确授权实际生成；本次任务是实现和发布 Skill，没有为测试项目伪造这些确认。内置 `image_gen` 的真实候选、用户选图、QA 返修和最终文件确认应在后续真实调用 `$build-image-fast` 时完成。

## 结论

V1 的结构、静态合同和股票四格状态机演练通过。真实端到端图片质量验收仍是明确的待验证项，不能据此宣称“真实出图已通过”。
